robot: Config/robot_template.yaml
sensors:
  - Config/sensor_template.yaml
inference:
  engine: rule_based
  params:
    obstacle_threshold: 1.0
    step_size: 0.4
safety:
  emergency_stop_enabled: true
  heartbeat_timeout_ms: 300

