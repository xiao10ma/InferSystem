# Example

Examples show how to compose sensors, robots, inference engines, and the SDK.

Current examples:

- `simple_runtime.py`: one robot with one sensor and one inference engine
- `fleet_demo.py`: two robots managed by one fleet wrapper

As the project grows, add examples for:

- Real hardware adapters
- Simulation integration
- Task-level planning
- Remote SDK or API usage

